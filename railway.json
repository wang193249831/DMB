{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python message_board/manage.py migrate && python message_board/manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn message_board.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
